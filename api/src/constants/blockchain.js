'use strict';

const NETWORK_TYPE = {
    MAINNET: "mainnet",
    TESTNET: "testnet",
    DEVNET: "devnet"
}

const PROTOCOL_TYPE = {
    UTXO_BASED: "utxo-based",
    ACCOUNT_BASED: "account-based"
}

module.exports = {
    NETWORK_TYPE,
    PROTOCOL_TYPE
};
